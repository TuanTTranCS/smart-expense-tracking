I plan to implement the Excel path update with Hermes agent plan using the Hermes agent itself. Help me to update the docs for that and draft a goal in docs/3.excel-update-hermes-goal.md. The hermes agent does not know about the Android app so a brief background in a goal would be helpful. You can look at the actual Excel file below to understand it better
My draft idea for the goal file:

- Create a cron job that run every 5 minutes
- Prioritize code execution to check for file status/results & content parsing, only use LLM if unsure
- Automatically locate the Onedrive root folder
- Check in the relative path from OneDrive root: Documents\2_Others\Expenses_finance\logs
  - If there is any .json file in that folder with file name contains the format expense_<time-stamp></time>_<Id></id> ( for example, expense_20260910_081308_f87c6a20.json), get the info following the json format of the Android app
- Find the Excel file "D:\OneDrive\Documents\2_Others\Expenses_finance\Canada plan.xlsx" (use relative OneDrive path
  - If the file is being opened, skip the current run
  - Else:
    - Find the proper sheet for the month of the receipt date info in the json file
    - If there is no sheet of that receipt date, create the new sheet for that month, copy the frame work (formula, fixed cells, place holder cells)
    - On the proper sheet, enter the receipt info in to the sheet at the appropriate cells: In column F,G,H, starting from row 12
      - Column F: amount
      - Column G: Store/shop name and receipt date. Supermarket can be short-written as Mart, and Date use format Mon DD (Look at the curren cell for more details)
